// ---------------------------
// 백엔드 주소 (개발용 하드코딩)
// ---------------------------

// 개발 환경에서 사용할 PC IP 주소
// 팀원 각자가 본인 PC IP만 수정하면 됨
// Windows: 명령 프롬프트에서 'ipconfig' 입력 후 IPv4 주소 확인
// Mac: 터미널에서 'ifconfig' 입력 후 en0 또는 en1 섹션의 inet 주소 확인
// ⚠️ 주의: 공용 네트워크에서는 동일 IP가 할당될 수 있으므로, 개인 핫스팟이나 사설 네트워크 사용 권장
// ⚠️ 주의: IP 변경 시 서버 재시작 필요
// ⚠️ 주의: 배포 시에는 고정 IP 또는 도메인 사용 권장
// ⚠️ 주의: 커밋 시 본인 IP가 노출되지 않도록 주의
export const PC_IP = "";

// API 엔드포인트 기본 주소
export const BASE_URL = `http://${PC_IP}:8000/api`;

// 이미지·파일 등 상대경로 보정용
export const SERVER_BASE = `http://${PC_IP}:8000`;
